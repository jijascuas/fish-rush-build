{
  "name": "fish-rush",
  "version": "1.0.0",
  "description": "Underwater avoid-em-up game",
  "main": "index.html",
  "dependencies": {
    "@capacitor-community/admob": "^6.2.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "scripts": {
    "build": "powershell -Command \"if (Test-Path www) { Remove-Item -Recurse -Force www }; New-Item -ItemType Directory -Path www; Copy-Item index.html, script.js, style.css, privacy-policy-en.html www/; Copy-Item assets -Destination www/ -Recurse\"",
    "assets": "npx @capacitor/assets generate --android",
    "sync": "npx cap sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jijascuas",
  "license": "ISC",
  "devDependencies": {
    "firebase": "^10.7.1"
  }
}

{
  "name": "fish-rush",
  "version": "1.0.0",
  "description": "Underwater avoid-em-up game",
  "main": "index.html",
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "scripts": {
    "build": "powershell -Command \"if (Test-Path www) { Remove-Item -Recurse -Force www }; New-Item -ItemType Directory -Path www; Copy-Item index.html, script.js, style.css, privacy-policy-en.html www/; Copy-Item js, assets -Destination www/ -Recurse\"",
    "assets": "npx @capacitor/assets generate --android",
    "sync": "npx cap sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jijascuas",
  "license": "ISC",
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "firebase": "^11.0.0"
  }
}
